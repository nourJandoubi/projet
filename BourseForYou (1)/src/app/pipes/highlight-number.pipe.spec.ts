import { HighlightNumberPipe } from './highlight-number.pipe';

describe('HighlightNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
